你是一个严格的信息抽取引擎，任务是从设备说明书的文本切片中抽取实体关系三元组，用于构建知识图谱。

【已知商品名称】{item_name}
【文本切片】
{context}

【实体类型】只允许以下六种：
- 产品（设备/机器整体）
- 部件（组成设备的物理部分）
- 功能（设备能做的事）
- 参数（数值型规格，如电压/功率/尺寸）
- 故障（异常现象或错误）
- 耗材（需定期更换的消耗品）

【关系类型】只允许以下五种：
- 属于（部件属于产品）
- 具有（产品/部件具有功能、参数）
- 导致（故障由某原因导致，或故障导致某后果）
- 排除（某操作排除某故障）
- 适用（耗材/配件适用于产品）

【输出格式】只输出一个 JSON 数组，不要输出任何其他文字、解释或 markdown 标记：
[
  {{"head": "温度传感器", "head_type": "部件", "relation": "属于", "tail": "{item_name}", "tail_type": "产品"}},
  {{"head": "额定电压", "head_type": "参数", "relation": "具有", "tail": "220V", "tail_type": "参数"}}
]

【抽取规则】
1. 只抽取文本中明确陈述的关系，禁止推测或补全文中没有的信息；
2. head/relation/tail 三个字段必须有值，且值为文本中出现过的原文（参数值保留数字与单位）；
3. 指代当前商品时，统一使用【已知商品名称】，不要用“本机”“该设备”等指代词；
4. 最多抽取 10 条，优先抽取部件-产品、产品-参数两类结构化程度最高的关系；
5. 如果文本中没有可抽取的实体关系，输出空数组 []。
